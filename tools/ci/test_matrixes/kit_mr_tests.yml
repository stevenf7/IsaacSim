.test-linux-matrix:
  parallel:
    matrix:
      - SUITE: pythontests
        BUCKET:
          - sensors


.test-windows-matrix:
    parallel:
      matrix:
        - SUITE: pythontests
          BUCKET:
            - sensors