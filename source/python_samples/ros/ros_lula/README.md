Quickstart:

Start roscore

From the base folder for the package, execute:

```./python.sh python_samples/ros/ros_lula/ros_lula.py```

From the lula_ros package execute:

```rosrun lula_ros lula_command_stream_interpolator```