.. note:: The "input body" outputs will tell you *which* of the physics bodies input triggered the node execution. If you only specify a single physics body input, this will always also be the inputBody output.

For an example of concrete use of contact event nodes within an ActionGraph, see the OmniGraph demo in the physics demo scenes under Contacts.